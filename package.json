{  
   "workspaces": [
      "packages/*"
   ],
   "scripts": {
      "pullpack": "packtool pull",
      "pushpack": "packtool push",
      "publish" : "npx lerna exec -- npm publish --access public" 
   },
   "devDependencies": {
      "@packtool/core": "0.2.0",
      "lerna": "^8.1.2"
   }
}
